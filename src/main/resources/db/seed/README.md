# Seed Data

This directory documents seed-data boundaries. Production schema changes and approved seed data must be applied through Flyway migrations.

Sprint 1 seed data is local-safe only and must not include real passwords, raw OTP values, production credentials, or final business data.
